import styles from './styles.module.css'

function Hero() {
  return (
    <section className={styles.hero}>
      <h1 className={styles.title}>Generate Design Tokens</h1>
      <p className={styles.subtitle}>Use the best tokens around. The tokens you already have.</p>
    </section>
  )
}

export default Hero
